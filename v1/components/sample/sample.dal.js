class Dal {
    getSample(text) {
        // TODO: Set up db connection
        return `The param is '${text}'`;
    }

    addSample(data) {
        // TODO: Set up db connection
        return data;
    }
}

module.exports = Dal;
